# DAILY SIGNAL — Requirements
# Python 3.11+ | Tested on GitHub Actions ubuntu-latest
# Semua versi dipilih berdasarkan kompatibilitas Python 3.11

# ── Core Data ─────────────────────────────────────────────────────
yfinance>=0.2.40
pandas>=2.1.0,<3.0.0
numpy>=1.26.0,<3.0.0
# CATATAN: pandas-ta DIHAPUS — tidak tersedia di PyPI untuk Python 3.11
#          Seluruh indikator TA (RSI, EMA, MACD, ADX, ATR, BB)
#          diimplementasikan sendiri secara pure numpy/pandas di src/signals/ta_engine.py

# ── Database ──────────────────────────────────────────────────────
supabase>=2.0.0
# psycopg2-binary DIHAPUS — supabase SDK sudah include driver sendiri

# ── HTTP ──────────────────────────────────────────────────────────
requests>=2.31.0

# ── Streamlit Dashboard ───────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0
# streamlit-extras DIHAPUS — tidak dipakai di kode

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
tenacity>=8.2.0
pytz>=2024.1
pydantic>=2.5.0
pydantic-settings>=2.2.0
loguru>=0.7.0

# ── PDF Generation (opsional, hanya untuk buat dokumen) ───────────
reportlab>=4.0.0

# ── Dev / Testing ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
